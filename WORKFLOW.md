# Workflow
```
workflow
  jenkins
    job
      start
  github
    pull_request
      status
      merge
    release
      create

  docker-compose
    build
    test
    package
```

# Virt-specific
```
docker
  pull
  build
  run

docker-compose
  pull
  buil
  run

vagrant
  up
  ssh --command ''
```
